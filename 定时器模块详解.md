# 定时器模块详解

## 1. 模块目标

这个项目里的“定时器模块”并不是通用定时任务系统，而是一个很聚焦的“连接空闲超时管理器”。

它只做一件事：

- 记录哪些客户端连接当前处于 keep-alive 空闲状态
- 判断这些连接是否已经超过空闲超时时间
- 超时后把连接从 `epoll` 中移除并关闭

它的存在是为了避免下面这种情况：

- 浏览器或客户端建立了 HTTP 长连接
- 服务器已经回完响应，但客户端迟迟不发下一个请求
- 如果服务器一直不回收，这些 fd 会长期占用内核和用户态资源

当前项目把“空闲 30 秒仍无新请求的 keep-alive 连接”视为超时连接。

相关常量在 [include/server.h](./include/server.h) 中定义：

```cpp
static const int kConnectionTimeoutSeconds = 30;
```

## 2. 先说结论：当前实际实现是什么

先讲最重要的一点，避免看文档时被旧描述误导。

当前代码里真正运行的定时器逻辑是：

- `unordered_map<int, time_point> active_timers_`
- `epoll_wait(..., 1000)` 每次最多阻塞 1 秒
- 每轮循环后调用 `check_timeout_connections()`
- 扫描 `active_timers_`，把已经超时的连接关闭

也就是说，当前实现本质上是：

```text
“哈希表记录过期时间” + “固定 1 秒周期检查”
```

而不是已经完全落地的：

```text
“小根堆 + 懒删除 + 动态 epoll 超时”
```

虽然 `Server` 里还保留了 `timer_heap_`、`TimerNode`、`TimerNodeCompare` 这些结构，但它们当前没有参与实际运行流程。

## 3. 相关数据结构

### 3.1 `active_timers_`

定义位置：[include/server.h](./include/server.h)

```cpp
std::unordered_map<int, std::chrono::steady_clock::time_point> active_timers_;
```

含义：

- key 是连接 fd
- value 是这个连接的“过期时刻”

如果某个 fd 出现在 `active_timers_` 中，表示：

- 这个连接当前处于“等待下一次请求”的空闲阶段
- 它正在被定时器模块监控

如果某个 fd 不在 `active_timers_` 中，通常表示：

- 它正在被当前线程或工作线程处理
- 或者连接已经被关闭

### 3.2 `conn_mtx_`

定义位置：[include/server.h](./include/server.h)

```cpp
std::mutex conn_mtx_;
```

作用：

- 保护 `active_timers_`
- 防止主线程和工作线程同时修改连接超时信息时发生竞态

### 3.3 `timer_heap_`

定义位置：[include/server.h](./include/server.h)

```cpp
std::priority_queue<TimerNode, std::vector<TimerNode>, TimerNodeCompare> timer_heap_;
```

这个结构从命名和注释上看，是为“小根堆方案”预留的。但在当前版本中：

- 没有地方向它 `push`
- 没有地方从它 `pop`
- `next_timeout_ms()` 也没有读取堆顶时间

所以你可以把它理解为：

- 当前版本尚未真正接入的设计残留
- 或者后续准备继续优化时留下的接口

## 4. 定时器相关核心函数

### 4.1 `refresh_conn_timer(int fd)`

定义位置：[src/server.cpp](./src/server.cpp)

```cpp
void Server::refresh_conn_timer(int fd) {
    std::lock_guard<std::mutex> lock(conn_mtx_);
    active_timers_[fd] =
        std::chrono::steady_clock::now() + std::chrono::seconds(kConnectionTimeoutSeconds);
}
```

作用：

- 给某个连接设置新的过期时间
- 本质上就是“延长寿命”

什么时候会调用：

- 新连接 `accept` 完并成功加入 `epoll` 后
- 一次请求处理完成，决定 keep-alive，并重新挂回 `epoll` 后

你可以把它理解成：

```text
“这个连接现在重新进入空闲等待状态了，从现在起再给它 30 秒寿命。”
```

### 4.2 `erase_conn_activity(int fd)`

定义位置：[src/server.cpp](./src/server.cpp)

```cpp
void Server::erase_conn_activity(int fd) {
    std::lock_guard<std::mutex> lock(conn_mtx_);
    active_timers_.erase(fd);
}
```

作用：

- 把某个连接从“空闲超时监控表”里移除

为什么要移除：

- 一旦 `epoll` 告诉我们这个连接可读，说明它不再是“空闲连接”
- 这时连接要进入业务处理阶段
- 如果不先移除，扫描超时时可能把一个正在处理的连接误杀

所以它的语义其实是：

```text
“这个 fd 已经开始干活了，先不要把它当作空闲连接来计时。”
```

### 4.3 `check_timeout_connections()`

定义位置：[src/server.cpp](./src/server.cpp)

```cpp
void Server::check_timeout_connections() {
    std::vector<int> expired_fds;
    {
        std::lock_guard<std::mutex> lock(conn_mtx_);
        auto now = std::chrono::steady_clock::now();
        for (auto it = active_timers_.begin(); it != active_timers_.end();) {
            if (it->second <= now) {
                expired_fds.push_back(it->first);
                it = active_timers_.erase(it);
            } else {
                ++it;
            }
        }
    }

    for (size_t i = 0; i < expired_fds.size(); ++i) {
        int fd = expired_fds[i];
        epoll_ctl(epfd_, EPOLL_CTL_DEL, fd, nullptr);
        close(fd);
    }
}
```

这个函数分成两步。

第一步：在锁内扫描 `active_timers_`

- 拿当前时间 `now`
- 找出所有 `expires_at <= now` 的 fd
- 从 `active_timers_` 里删掉
- 把这些过期 fd 收集到 `expired_fds`

第二步：在锁外关闭连接

- 从 `epoll` 中删除 fd
- 调用 `close(fd)`

为什么要“先收集、后关闭”：

- 关闭 fd 属于相对更重的系统调用
- 如果在持锁状态下直接 `epoll_ctl + close`，锁会持有更久
- 当前写法能缩短 `conn_mtx_` 占用时间

### 4.4 `next_timeout_ms()`

定义位置：[src/server.cpp](./src/server.cpp)

```cpp
int Server::next_timeout_ms() {
    return 1000;
}
```

这个函数名字看起来像是“算出下一个真正的超时时刻”，但当前版本没有这么做。

它现在只是简单返回 `1000`，含义是：

- `epoll_wait` 最长睡 1000ms
- 最多每 1 秒醒一次做一轮超时检查

所以当前定时器的检查精度大致是“秒级”而不是“精确到最近一个连接过期点”。

## 5. 定时器是怎么嵌进主流程的

### 5.1 新连接建立时

在 `run()` 的 `accept` 分支里，新连接成功加入 `epoll` 后会调用：

```cpp
refresh_conn_timer(client_fd);
```

这表示：

- 连接虽然刚建立，但如果客户端迟迟不发请求，也不能一直挂着
- 所以一接入就给它一个 30 秒空闲倒计时

### 5.2 连接变为可读时

在 `EPOLLIN` 分支里，主线程会先调用：

```cpp
erase_conn_activity(client_fd);
```

这一步非常关键。

它表示：

- 当前连接已经从“空闲等待下一次请求”的状态
- 切换到“正在读请求/正在处理请求”的状态

也就是说，只有“空闲中的 keep-alive 连接”才应该被定时器管理，正在处理的连接不应该继续倒计时。

### 5.3 请求处理完成且继续 keep-alive 时

如果响应发送成功，并且 HTTP 语义允许 keep-alive，代码会：

```cpp
rearm_conn_fd_in_epoll(client_fd);
refresh_conn_timer(client_fd);
```

意思是：

- 重新把 fd 设回 `EPOLLONESHOT` 的可监听状态
- 同时重新开始下一轮 30 秒空闲倒计时

这是定时器模块和 keep-alive 结合得最紧的地方。

### 5.4 请求处理完成但不再保持连接时

如果本次请求之后不 keep-alive，代码会直接：

- `close(client_fd)`
- `erase_conn_activity(client_fd)`

这种情况下连接生命周期结束，不再需要定时器管理。

## 6. 事件循环里的执行节奏

主循环的关键逻辑在 `Server::run()`：

```cpp
int nfds = epoll_wait(epfd_, events, MAX_EVENTS, next_timeout_ms());
check_timeout_connections();
```

把它翻译成白话就是：

1. `epoll_wait` 最多阻塞 1 秒
2. 如果 1 秒内有网络事件，就提前醒来
3. 醒来后先做一轮超时检查
4. 再处理本轮收到的网络事件

所以当前定时器模块不是“独立线程 + sleep”的模式，而是：

```text
复用 epoll 主循环的节奏做周期性检查
```

这也是它比较轻量的地方：

- 没有单独的 timer 线程
- 没有额外的复杂调度器
- 直接借助主事件循环完成超时回收

## 7. 一次完整的生命周期示例

假设客户端 A 建立了一个 keep-alive 连接。

### 阶段 1：刚连接上来

- 主线程 `accept`
- `add_conn_fd_to_epoll(client_fd)`
- `refresh_conn_timer(client_fd)`

此时 `active_timers_[fd] = now + 30s`

### 阶段 2：客户端发来请求

- `epoll_wait` 返回 `EPOLLIN`
- 主线程先 `erase_conn_activity(fd)`

此时这个 fd 从超时表中移除，避免处理期间被误判为超时。

### 阶段 3：业务处理与响应发送

- Reactor 模式：工作线程负责读 + 处理 + 写
- 模拟 Proactor 模式：主线程先读，工作线程再处理 + 写

不管哪种模式，进入业务处理阶段后，这个连接都不在 `active_timers_` 中。

### 阶段 4：响应发完，决定继续 keep-alive

- `rearm_conn_fd_in_epoll(fd)`
- `refresh_conn_timer(fd)`

此时这个 fd 再次进入空闲等待状态，新的过期时间变成“当前时刻 + 30 秒”。

### 阶段 5：客户端长时间不发新请求

- 主循环每隔最多 1 秒醒一次
- `check_timeout_connections()` 扫描 `active_timers_`
- 发现该 fd 已经过期
- `epoll_ctl(DEL)` + `close(fd)`

至此，这个空闲连接被回收。

## 8. 为什么这里要用 `steady_clock`

过期时间使用的是：

```cpp
std::chrono::steady_clock::time_point
```

而不是系统墙上时钟。

原因是：

- `steady_clock` 单调递增
- 不受系统时间被手动修改、NTP 校时、时区变化影响

对于“多久之后超时”这类相对时间判断，`steady_clock` 比 `system_clock` 更稳妥。

## 9. 当前实现的复杂度

### 9.1 刷新定时器

`refresh_conn_timer(fd)` 的核心是：

```cpp
active_timers_[fd] = expires_at;
```

平均复杂度可以看作 `O(1)`。

### 9.2 移除定时器

`erase_conn_activity(fd)` 的核心是：

```cpp
active_timers_.erase(fd);
```

平均复杂度也是 `O(1)`。

### 9.3 超时检查

`check_timeout_connections()` 需要遍历整个 `active_timers_`：

```text
O(n)
```

这里的 `n` 是“当前处于空闲等待状态的连接数”。

所以当前版本的代价主要集中在：

- 空闲连接很多时，每秒都要全表扫描一次

## 10. 当前方案的优点

### 10.1 实现简单

读代码很直接：

- 一个表记录超时时间
- 一轮扫描找出过期连接

对学习项目很友好，调试成本也低。

### 10.2 容易和 `epoll` 主循环融合

不需要再开一个 timer 线程，也不需要额外引入 Linux `timerfd`。

### 10.3 逻辑上比较安全

通过“处理前先 `erase`，处理完成后再 `refresh`”这组动作，能把：

- 正在处理中的连接
- 空闲等待中的连接

清楚地区分开。

## 11. 当前方案的局限

### 11.1 仍然是线性扫描

只要有很多 keep-alive 空闲连接，`check_timeout_connections()` 就要每轮遍历整个表。

这意味着：

- 连接规模越大，扫描成本越高
- 即使本轮其实只有极少数连接过期，也得把表扫一遍

### 11.2 `next_timeout_ms()` 还没有真正动态化

虽然函数名叫 `next_timeout_ms()`，但当前直接返回 `1000`。

因此它还没有做到：

- 根据最近一个连接的过期时刻精确设置 `epoll_wait` 超时时间

### 11.3 头文件里的“小根堆设计”尚未落地

目前 `timer_heap_` 只是声明了，没有真正参与逻辑。

这会让读代码的人产生一种错觉：

- 头文件像是堆方案
- 实际运行却是哈希表扫描方案

这也是为什么项目文档需要特别说明“当前实际实现”的原因。

## 12. 如果以后升级成“小根堆 + 懒删除”，通常会怎么做

一个更高效的方向通常是：

1. `refresh_conn_timer(fd)` 时把新的 `(expires_at, fd)` 压入小根堆
2. 同时在 `active_timers_` 里记录这个 fd 当前最新的过期时刻
3. `next_timeout_ms()` 读取堆顶，算出离最近超时还剩多少毫秒
4. `check_timeout_connections()` 只反复弹出堆顶那些已经到期的节点
5. 如果堆顶节点不是该 fd 的“最新版本”，就把它当成旧节点丢弃，这就是懒删除

这样可以把：

- 刷新定时器：变成 `O(log n)`
- 查找最近超时：变成 `O(1)`
- 处理到期连接：接近“按过期数量收费”

这会比“每秒全量扫描”更适合大量长连接场景。

## 13. 当前实现和业务语义之间的关系

一定要注意，这个定时器管的是：

```text
连接空闲时间
```

不是：

- 单个请求处理超时
- MySQL 查询超时
- 写回响应超时
- 整个业务任务执行超时

所以它的边界很清晰：

- 连接空闲时计时
- 连接忙碌时不计时

这是一个“keep-alive 空闲回收器”，不是通用任务超时框架。

## 14. 一句话总结

当前项目的定时器模块，本质上是一个基于 `unordered_map + steady_clock + epoll 主循环周期检查` 的空闲连接回收机制。

它的核心策略是：

- 空闲时登记过期时间
- 有请求到来时先取消计时
- 请求处理结束且继续 keep-alive 时重新计时
- 主循环每秒扫描一次，回收超时连接

它的优点是简单、清晰、轻量，缺点是空闲连接很多时需要 `O(n)` 扫描。头文件里虽然已经预留了小根堆相关结构，但当前版本的实际运行逻辑还没有切换到堆方案。
